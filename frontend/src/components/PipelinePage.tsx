import { PipelinePanel } from './PipelinePanel'

/** The scout/research pipeline, migrated from Market-AI (P4). Approved repos
 *  get a per-row "Add" button that launches the onboarding wizard. */
export function PipelinePage() {
  return <PipelinePanel />
}
